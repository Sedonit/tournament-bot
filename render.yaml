# render.yaml
services:
  - type: service  # <<< ИЗМЕНЕНО С web НА service
    name: tournament-bot
    runtime: python
    plan: free     # <<< Рекомендуется для предотвращения множественных реплик
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py